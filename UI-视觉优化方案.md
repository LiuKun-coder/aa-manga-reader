# 漫画阅读器 UI 视觉优化方案

> 基于 Claude 设计语言的温暖、编辑式美学，为安卓端漫画阅读器注入一份「值得停留」的品质感。
> 本文档仅提供设计方向与视觉规范，不包含任何实现代码。

---

## 1. 整体视觉诊断

通过阅读当前项目的样式定义与布局结构，可以提炼出以下几类视觉问题：

**色彩：深色背景缺乏温度与层次**

当前书库页使用纯黑背景 (`#000`)，搭配 `rgba(255,255,255,0.08)` 等低透明度白色作为卡片与输入框底色。这种处理方式在 OLED 设备上虽然省电，但视觉上显得冷硬、扁平，缺少「深度感」。纯黑与纯白的搭配缺少一个有辨识度的品牌色来锚定用户的视觉记忆——当前蓝色筛选标签 (`rgba(100,160,255,0.85)`) 与整体冷调暗色搭配虽然功能清晰，但不够独特。

**层次：卡片与背景的分离感不足**

列表项 (`folder-item`) 和历史卡片的背景透明度差异极小（`0.08` vs `0.06`），卡片与背景之间的边界模糊。缺少阴影或边框来构建纵向的「层叠关系」，导致整个页面看起来像是在同一平面上铺开的，信息没有主次之分。

**留白：间距缺乏呼吸节奏**

书库页顶部内边距为 `80rpx`（约 40dp），列表项之间的间距为 `16rpx`（约 8dp），历史卡片间距也为 `16rpx`。整体间距偏紧，尤其是列表项之间的分隔仅靠半透明底色区分，视觉上容易产生「拥挤」的感觉。搜索框与筛选条之间的垂直间距同样偏小，功能区之间的层次不够清晰。

**字体：字号层级不够鲜明**

当前标题（`folder-name`）为 `30rpx`（约 15sp），辅助文字为 `24rpx`（约 12sp），历史卡片标题更小至 `22rpx`。虽然存在大小差异，但字重均为系统默认，没有通过 weight 的变化来强化层级。这让标题与正文之间的对比仅靠 2-3sp 的差异来传达，在快速浏览时信息层次不够分明。

**图标：使用 Emoji 作为占位图标**

当前封面缺失时使用 📖 和 📄 作为占位符。Emoji 在不同设备上的渲染效果不一致（尤其是在安卓上），且风格与自定义 UI 不协调。一个精心设计的占位图形能传达「这里将来会有内容」的期待感，而 Emoji 更像是「还没做完」。

**对齐与一致性**

部分组件的圆角值不统一：卡片 `12rpx`、搜索框 `12rpx`、历史封面 `8rpx`、进度胶囊 `24rpx`、弹窗 `20rpx`。圆角值的随意跳跃会让界面失去整体感，用户虽然不一定能说出来，但会隐约觉得「哪里不太对」。

---

## 2. 设计语言与风格方向

### 推荐风格：温暖暗色编辑式 (Warm Dark Editorial)

这是一种融合了 Claude 设计系统精髓与现代漫画阅读场景的风格方向。

**核心印象：** 打开 App 的那一刻，你会感觉自己走进了一家灯光暖黄、书架整齐的独立书店。它不是冰冷的工具，而是一个有温度的空间——深棕色的背景不是「黑屏」，而像是深色木质书架；陶土色的点缀不是「功能色」，而是书店门口那盏温暖的灯。

**为什么选择这个方向：**

- 漫画阅读本质上是一种沉浸式的、私密的体验，暖色调暗色模式比冷色调更让人放松、更愿意停留
- Claude 设计系统的核心色板（`#D97757` 陶土橘、`#FAF9F5` 暖白、`#1B1B19` 深棕黑）天然适合这种场景
- 「编辑式」意味着排版讲究、留白有节奏，像一本精心设计的杂志而非一个功能堆砌的工具
- 在 Material Design 3 的框架下，这种风格可以通过 Tonal Surface 系统和动态色彩轻松实现

**视觉关键词：** 温暖、沉浸、精致、安静

### 备选风格：暗色玻璃态 (Dark Glassmorphism)

如果未来想要更前卫的视觉方向，可以考虑在暗色基础上加入毛玻璃效果——弹窗、导航栏、筛选条使用半透明磨砂背景，配合微妙的边框光晕。但这个方向在安卓低端设备上的性能开销需要额外考量，因此作为备选而不作为首推。

---

## 3. 色彩系统优化

### 主色板

| 用途 | 色名 | HEX 值 | 使用场景 |
|------|------|--------|----------|
| 品牌主色 | 陶土橘 | `#D97757` | 仅用于最关键的操作（如「开始阅读」按钮）、品牌标识、进度高亮 |
| 品牌主色浅 | 浅陶土 | `#E8A98F` | 图标选中态、轻量级高亮 |
| 品牌主色深 | 深陶土 | `#8A5740` | 按压态、选中文字 |
| 强调蓝 | 阅读蓝 | `#64A0FF` | 可保留用于阅读进度和次要信息高亮，但应降低使用频率 |

### 背景色（Tonal Surface 层级，由浅到深）

| 层级 | HEX 值 | 用途 |
|------|--------|------|
| Surface 0（最深） | `#141210` | 页面主背景、阅读器底色 |
| Surface 1 | `#1C1A17` | 列表项底色、卡片底色 |
| Surface 2 | `#252320` | 弹窗背景、筛选条底色 |
| Surface 3 | `#2E2C28` | 搜索框底色、输入框底色 |
| Surface 4 | `#383530` | 悬浮按钮、工具栏高亮态 |

> 这组背景色的灵感来自 Claude 设计系统的 `bg-50` 到 `bg-700` 色阶，加入了微妙的暖棕底调（RGB 中 B 通道略低），让暗色背景不再是纯中性灰黑，而是带有「纸张」或「木质」的温暖气质。

### 文字色

| 层级 | HEX 值 | 字重建议 | 用途 |
|------|--------|----------|------|
| 文字主色 | `#FAF9F5` | Regular/Medium | 标题、主要信息 |
| 文字次色 | `#B7B5A9` | Regular | 副标题、辅助说明 |
| 文字弱色 | `#6E6D68` | Regular | 时间戳、占位提示 |
| 文字禁用 | `#46443B` | Regular | 不可操作状态 |

### 功能色

| 用途 | HEX 值 | 说明 |
|------|--------|------|
| 成功 | `#8CA06F` | 缓存完成、操作成功提示 |
| 警告 | `#E8C86A` | 存储空间不足、权限提醒 |
| 错误 | `#EF4444` | 文件损坏、删除确认 |
| 信息 | `#64A0FF` | 阅读进度、次要提示 |

### 浅色主题适配思路

浅色主题应保持同一套色相关系，只是明度反转：

- 背景：`#FAF9F5`（暖白）→ `#F1F1EF`（次级）→ `#E9E6DC`（卡片）
- 文字主色：`#141413`，次色：`#46443B`
- 品牌主色 `#D97757` 不变，但在浅色背景上需要加深到 `#B0683F` 以保证对比度

### 色彩使用原则

1. **主色克制的力量**：品牌主色（陶土橘）只在页面上出现 1-2 处。它的稀缺性让每次出现都有分量——如果到处都是主色，它就失去了引导视线的能力
2. **背景保持中性偏暖**：大面积的背景色使用 Surface 色阶，不要使用高饱和度色彩
3. **功能色按语义使用**：红色只用于破坏性操作或错误状态，绿色只用于成功反馈，蓝色降级为信息色而非主色
4. **透明度辅助，但不作为主手段**：可以用 `rgba` 做叠加态和按压态，但不要用极低透明度的白色 (0.06-0.08) 来定义整个视觉层次——这会让界面看起来「脏」

---

## 4. 字体与排版体系

### 推荐字体家族

| 平台 | 推荐字体 | 备选 |
|------|----------|------|
| Android | `Noto Sans CJK SC`（思源黑体） | 系统默认 `Roboto` |
| iOS（如未来适配） | `SF Pro Text` + `PingFang SC` | - |
| 数字/英文 | `Roboto` 或 `DIN Alternate` | 系统默认 |

> 思源黑体在安卓上渲染质量高、字重齐全、风格中性且温暖，与 Claude 设计系统的 Poppins（西文）气质吻合。不推荐引入额外的字体文件以避免包体积膨胀。

### 排版层级

| 层级 | 字号 (sp) | 字重 | 行高 | 字间距 | 用途 |
|------|-----------|------|------|--------|------|
| Display | 24 | Medium (500) | 1.2 | +0.5% | 页面大标题（如「书库」） |
| Title 1 | 20 | SemiBold (600) | 1.25 | 0 | 漫画标题（列表项主标题） |
| Title 2 | 17 | Medium (500) | 1.3 | 0 | 弹窗标题、分区标题 |
| Body | 15 | Regular (400) | 1.5 | 0 | 正文、描述文字 |
| Caption | 13 | Regular (400) | 1.4 | 0 | 辅助信息、时间戳 |
| Overline | 11 | SemiBold (600) | 1.2 | +2% | 标签、筛选 Chip |

> **rpx 适配思路**：以 375dp 设计稿宽度为基准，1sp ≈ 2rpx。例如 15sp 正文在 uni-app 中写为 `30rpx`。实际开发中可通过 `uni.upx2px()` 动态转换，或直接使用 sp 单位（推荐），因为 uni-app 在 App 端支持 sp。

### 排版层级原则

- **至少 2sp 以上的字号差**才能建立清晰的层级关系。`22rpx` 与 `24rpx` 之间仅差 1sp，在快速浏览时几乎无法区分
- **字重比字号更有效**：同样 15sp 的文字，SemiBold 和 Regular 之间的视觉差异比 15sp 和 17sp 之间更大。善用 `font-weight: 600` 来强化关键信息
- **行高与字号匹配**：标题行高紧凑（1.2-1.3），正文行高宽松（1.5-1.6），这样标题看起来更有力量感，正文更容易阅读
- **字间距谨慎使用**：仅在大写标签 (Overline) 中使用正字间距，正文保持 0 以避免破坏中文阅读节奏

---

## 5. 间距与网格规范

### 基于 8dp 的全局间距系统

Material Design 3 推荐使用 8dp 为基础单位的间距系统。以下所有数值均为 dp 值。

| 名称 | 数值 (dp) | 用途 |
|------|-----------|------|
| `space-1` | 4 | 图标与文字之间的最小间距、紧凑型内边距 |
| `space-2` | 8 | 列表项内部元素的间距、筛选 Chip 内边距 |
| `space-3` | 12 | 卡片内部内边距（垂直方向）、组件间次要间距 |
| `space-4` | 16 | 页面水平边距（默认）、列表项之间间距、卡片内部内边距（水平方向） |
| `space-5` | 20 | 功能区之间的间距（如搜索栏与筛选条之间） |
| `space-6` | 24 | 页面水平边距（宽松）、分区标题与内容之间 |
| `space-8` | 32 | 页面顶部安全区域下方间距、大面积留白 |
| `space-10` | 40 | 页面底部安全区域上方间距 |

### 具体应用规则

- **页面水平边距**：`16dp`（标准）或 `24dp`（大屏/宽松模式）。不要使用 `32rpx`（≈16dp 偏小），建议统一为 `32rpx` = 16dp 或 `48rpx` = 24dp
- **列表项内边距**：垂直 `12dp`，水平 `16dp`。比当前的 `20rpx×24rpx`（≈10dp×12dp）更宽敞
- **列表项间距**：`8dp`。当前为 `16rpx`（≈8dp）恰好，但建议配合更明显的卡片底色差异来强化分隔
- **卡片圆角内部内边距**：不小于 `12dp`，确保内容不贴边
- **导航栏与内容区间距**：`0dp`（导航栏融入页面，不另加间距）

### 呼吸感的制造方法

1. **不等距原则**：上方间距略大于下方间距（如顶部 24dp + 底部 16dp），让视觉重心稳定在页面偏上位置
2. **分组间距 > 组内间距**：例如「继续阅读」区与「全部漫画」列表之间留 `24dp`，而列表项之间仅留 `8dp`。这组 `3:1` 的间距比天然形成了信息分组
3. **避免所有间距都一样**：如果每个元素之间的间距都相同，就等于没有层级。用 8dp / 16dp / 24dp 三档间距来表达「组内关系」→「组间关系」→「页面分区」三个层次

---

## 6. 关键组件视觉升级

### 6.1 按钮

**主按钮（如「开始阅读」「前往授权」）**

- 形状：圆角胶囊形，圆角 `999dp`（全圆角）
- 尺寸：高度 `48dp`，水平内边距 `24dp`
- 色彩：填充品牌主色 `#D97757`，文字白色 `#FAF9F5`
- 阴影：默认态使用 Claude 设计系统的 `shadow-sm`（双层柔和阴影），悬浮态提升至 `shadow-md`
- 按压态：色彩加深至 `#C96442`，阴影收至 `shadow-xs`（产生「按下」的触觉暗示）
- 禁用态：背景 `#2E2C28`，文字 `#6E6D68`，阴影消失

**次要按钮（如「去授权」）**

- 形状：圆角 `12dp` 矩形
- 色彩：填充 `#2E2C28`，文字 `#FAF9F5`
- 边框：`1dp` 实线 `#4A4A43`
- 按压态：背景加深至 `#383530`

**文字按钮（如「清空」）**

- 无背景、无边框、无阴影
- 文字色 `#B7B5A9`，字号 13sp
- 按压态：文字色变为 `#D97757`（给用户一个「被选中了」的反馈）

### 6.2 输入框与表单

**搜索框**

- 样式：底色 `#2E2C28`（Surface 3），无边框，圆角 `12dp`
- 高度：`44dp`（符合 MD3 触摸目标最小尺寸）
- 内边距：左侧放搜索图标后 `12dp`，右侧 `16dp`
- 文字色 `#FAF9F5`，占位符 `#6E6D68`
- 聚焦态：底部出现 `2dp` 高的品牌色下划线 `#D97757`，或者边框变为 `#D97757`（二选一，推荐下划线更克制）
- 无需传统聚焦时的外发光——那是旧版 Material 的做法，MD3 已经弃用

**页码跳转输入框**

- 与搜索框风格统一，但聚焦态保持底部下划线即可
- 错误态（输入超出范围）：底部下划线变红 `#EF4444`，下方出现红色提示文字 12sp

### 6.3 卡片与容器

**列表项卡片（漫画条目）**

- 圆角：统一 `12dp`
- 背景：`#1C1A17`（Surface 1），与页面主背景 `#141210` 之间有清晰的明度差
- 阴影：无阴影（列表项不需要阴影来强调，色彩差异足够）
- 内边距：垂直 `12dp`，水平 `16dp`
- 内容排列：封面在左（`80dp × 112dp`，3:4.2 比例接近漫画书），信息区在右（标题 + 进度 + 类型标签）
- 按压态：背景提亮至 `#252320`

**历史卡片**

- 封面尺寸：`64dp × 88dp`（保持 3:4.2 比例但更紧凑）
- 圆角：`8dp`（小卡片用小圆角，与列表项的 12dp 形成尺寸上的视觉呼应）
- 封面阴影：`shadow-xs`（极轻的阴影让封面「浮」起来，暗示可点击）

**弹窗容器**

- 圆角：`20dp`（大圆角让弹窗看起来更友好、更现代）
- 背景：`#1C1A17`
- 阴影：`shadow-xl`（弹窗需要强阴影来明确「浮在页面上层」）
- 内边距：顶部 `24dp`，底部 `16dp`，水平 `24dp`

### 6.4 导航栏与底部 Tab

**顶部工具栏（阅读器内）**

- 背景处理：使用 `#141210` 配合底部一条 `0.5dp` 的 `#2E2C28` 分割线，或使用 `Scrim`（从顶部向下渐变，从 `#141210` 到透明的 60dp 渐变条）
- 「返回书库」按钮：胶囊形，背景 `#252320`，圆角 `999dp`
- 文字色 `#FAF9F5`，字号 13sp，字重 Medium
- 推荐加上半透明模糊效果（`backdrop-filter: blur(12dp)`），但在安卓低端机上可降级为纯色

**底部进度条（阅读器内）**

- 背景：`#252320` 配合 `shadow-sm`
- 文字色 `#B7B5A9`，字号 13sp
- 圆角：`999dp`
- 增加一条微型进度指示条：在胶囊底部用品牌色 `#D97757` 绘制一条 `2dp` 高、宽度按当前页码/总页数比例的进度条

**筛选 Chip 栏**

- 未选中态：背景 `#252320`，文字 `#908E84`，无边框
- 选中态：背景 `#3A2A22`（品牌主色的极深色调），文字 `#D97757`（品牌主色），这样选中态既不突兀又清晰可辨
- Chip 内边距：垂直 `6dp`，水平 `16dp`
- Chip 间距：`8dp`
- Chip 圆角：`999dp`

### 6.5 图标体系

**风格建议：线性图标 (Outline)**

- 推荐使用线性风格（描边而非填充），线宽 `1.5dp`
- 线性图标在暗色模式下比面性图标更优雅、更轻量，不会「抢」封面的视觉权重
- 图标颜色：默认 `#908E84`，选中/激活态 `#D97757`

**统一规则**

- 所有图标使用相同的线宽和圆角半径
- 圆角连接处（linecap / linejoin）统一为 `round`
- 图标尺寸视差：
  - 导航级图标（返回、菜单）：`24dp`
  - 列表内图标（类型标签、状态）：`16dp`
  - 空状态插图：`64-96dp`

**封面占位图标**

- 替换 📖 和 📄 Emoji
- 使用一个简洁的线性书本图标（打开的书本轮廓），颜色 `#46443B`
- 可以在占位图中间加一条极淡的对角线装饰，暗示「封面即将到来」

### 6.6 加载态与空状态

**加载态**

- 全屏加载：使用品牌色 `#D97757` 的呼吸脉冲动画（圆点从透明到不透明循环），而非传统的旋转菊花
- 列表骨架屏：用 Surface 2 色 `#252320` 的圆角矩形条模拟封面和标题的形状，配合从左到右的微光扫过动画（shimmer）
- 骨架屏的圆角应与真实组件一致（封面骨架 `8dp` 圆角，标题骨架 `4dp` 圆角）

**空状态**

- 插图描述：一个简洁的线描书架，上面只有一两个书脊，旁边散落着一个放大镜。整个画面用 `#46443B` 单色绘制，风格温暖而不过于卡通
- 主文字：「书架空空如也」，字号 17sp，颜色 `#B7B5A9`
- 辅助文字：说明如何添加漫画的步骤，字号 13sp，颜色 `#6E6D68`
- 情感化处理：可以在文字下方加一句轻松的引导语，如「每一本好书都在等你发现」，字号 13sp，颜色 `#6E6D68`，字体使用 Serif（如果可用）来增加一点文学气息

---

## 7. 关键页面重塑蓝图

### 7.1 书库页（首页）

**整体布局：**

页面从上到下分为三个清晰的地带——

1. **顶部呼吸区**（安全区 + 标题 + 搜索）：状态栏下方留出 `24dp` 的呼吸空间，然后是页面标题「书库」（Display 级，24sp，SemiBold，暖白色）。标题下方紧接着搜索框，搜索框底色比页面背景高一个 Tonal 层级，形成一个「凹陷的搜索槽」的感觉。

2. **中间功能区**（历史 + 筛选 + 列表）：
   - 「继续阅读」区域：如果存在历史记录，用 Overline 级小标签「继续阅读」作为分区标题（陶土橘色，字间距 +2%），下方是水平滚动的历史封面卡片。每个卡片封面下方显示漫画名（单行截断），封面右上角叠加一个半透明的品牌色圆形进度指示器（`#D97757` 配合 0.2 透明度背景）。
   - 筛选 Chip 栏：放在「全部漫画」分区标题的右侧或下方，不单独占一行以节省垂直空间。
   - 漫画列表：每个列表项是一张「微卡片」，左侧封面带极轻阴影，右侧信息区分为标题（Title 1）和进度（Caption）。列表项底色使用 Surface 1，与 Surface 0 背景之间形成微妙的层级浮起感。

3. **底部安全区**：自然留白，不需要额外元素。

**视觉重心**：用户打开页面时，视线首先被「继续阅读」区域的封面横向滚动吸引——这些封面最近被阅读过，颜色鲜明、尺寸适中。然后视线自然向下移到搜索框（凹陷的视觉槽暗示「从这里开始」），最后滑到列表区域。

**浏览动线**：横向的历史卡片 → 向下扫描的纵向列表 → 点击进入阅读。整个动线是「先唤醒记忆，再探索新书」的自然流程。

### 7.2 阅读器页

**整体布局：**

阅读器应是最简洁的页面——漫画内容占据全部视觉空间，控制元素在非交互时完全隐藏。

1. **纯净阅读区**：背景 `#000000`（纯黑，为了最大化对比度，这是全 App 唯一使用纯黑的地方）。图片居中显示，不留多余边距。

2. **浮动控制层**：点击屏幕上 1/4 区域呼出顶部工具栏，点击下 1/4 区域呼出底部进度。控制层使用 Scrim 渐变（从边缘的 `#141210` 渐变到透明），让控制按钮「从黑暗中浮现」而非硬切。

3. **底部进度胶囊**：除了当前的页码文字 `「12 / 256」`，建议增加一条微型彩色进度条。胶囊底部用品牌色 `#D97757` 绘制一条 `2dp` 高的细线，宽度为 `(当前页/总页) × 胶囊宽度`。这条极细的彩色线在黑色背景上格外醒目，给用户一种「在旅程中」的安心感。

4. **横/竖切换按钮**：从当前的文字按钮改为图标按钮（双箭头旋转图标），视觉上更紧凑、更直观。

**视觉重心**：毫无疑问是漫画内容本身。控制元素的设计目标是「需要时瞬间出现，不需要时完全消失，且出现时也不抢漫画的注意力」。

---

## 8. 情感化细节与微交互建议

### 水波纹反馈 (Ripple)

- **触发时机**：所有可点击元素（列表项、按钮、Chip）
- **效果描述**：点击时从触点位置向外扩散一层极淡的品牌色水波纹（`#D97757` 透明度 0.15），持续 300ms 后淡出。在暗色背景上，这个波纹就像石子投入墨水湖面——沉静中有一丝涟漪
- **注意**：波纹半径不要超过组件边界，否则会显得粗糙

### 列表项的微弹性

- **触发时机**：手指按下列表项时
- **效果描述**：列表项在按下时沿 Y 轴微缩 `0.5%`（几乎察觉不到但能感受到「被按下了」），松开时弹性恢复。这不是明显的弹跳，而是像按下一个真实的按钮时微弱的阻力反馈

### 空状态插图的呼吸动画

- **触发时机**：空状态页面持续显示时
- **效果描述**：空状态的线描书架插图有一个极缓慢的上下浮动动画（每 4 秒一个周期，位移 4dp），就像书本在轻轻呼吸。这不是为了吸睛，而是让空页面不显得「死板」，暗示「这个空间是活的、在等你的」

### 下拉刷新的过渡

- **触发时机**：书库页下拉刷新时
- **效果描述**：刷新指示器使用品牌色 `#D97757` 的小圆点脉冲（3 个圆点依次从左到右亮起），取代传统的旋转菊花。刷新完成后，列表内容从顶部以 `fade-in + slide-up` 的方式出现（透明度从 0 到 1，同时 Y 轴位移 8dp），营造内容「从书架上被取下来」的感觉

### 进度胶囊的滑动反馈

- **触发时机**：用户点击底部进度胶囊跳转到页码时
- **效果描述**：胶囊内的微型进度条在页码切换时不是硬切，而是以 200ms 的 ease-out 动画滑到新位置。这个细节会让翻页动作有一种「连续的旅程感」而非「跳转」

### 历史卡片的入场

- **触发时机**：书库页加载完成后
- **效果描述**：历史卡片从右侧依次滑入（每张卡片延迟 50ms），形成一排封面「从书架上被推出来」的微动画。整个动画在 400ms 内完成，不会让用户等待

### 弹窗的 Material Motion

- **触发时机**：页码跳转弹窗和长按操作弹窗出现/消失时
- **效果描述**：弹窗从页面中心以 `scale(0.92) → scale(1)` + `opacity(0 → 1)` 的方式出现（200ms，ease-out），消失时反向播放。遮罩层同步从透明到半透明黑色。这种「从中心绽放」的出场方式比从底部滑入更聚焦、更有仪式感

---

## 9. 设计原则总结

1. **克制的品牌色**：主色出现的频率越低，它每次出现时的视觉分量就越重。把 `#D97757` 当作 salt，而不是 sauce
2. **暖色中性底**：大面积使用带暖调的暗灰色系，让暗色模式不再冷冰冰，而是有「书架」的温度
3. **层级靠明度差，不靠透明度**：用明确的 Tonal Surface 色阶区分背景层级，而不是在同一个纯黑背景上叠加不同透明度的白色——后者会让界面看起来脏、不清晰
4. **圆角统一律**：全 App 只使用 3 个圆角值——`8dp`（小元素/封面）、`12dp`（卡片/输入框）、`20dp`（弹窗/大容器）。消除当前圆角值的随意跳跃
5. **字重比字号更有效**：用 SemiBold 标题搭配 Regular 正文，比单纯放大字号更能建立清晰的阅读层级
6. **间距表达层级**：组内 8dp、组间 24dp、页面级 40dp——三档间距比构成了页面的信息架构
7. **动画服务于叙事**：每个微交互都应该讲述一个小故事（「被按下了」「内容来了」「正在为你加载」），而不是为了动而动。动画时长控制在 200-400ms，缓动曲线优先使用 ease-out
8. **阅读器就是内容本身**：阅读器页面的所有设计决策都服务于一个目标——让漫画成为唯一的视觉焦点。控制层应该是「隐形但可靠」的
9. **一致性是信任的基础**：当用户发现搜索框、弹窗、列表项使用同一套圆角、阴影、色阶时，他们会潜意识地觉得「这个 App 很专业、很可靠」

---

> 这份方案的核心出发点是：一本好的漫画值得一个与之匹配的阅读空间。我们不是在做一个「能用」的工具，而是在打造一个让人愿意打开、愿意停留、愿意反复回来的阅读角落。
